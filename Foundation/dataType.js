//1 숫자

let = 3; 

//2 문자열

let str = "hello";
let str2 = "Single quotes are ok too"
let str3 = "sum+ ${str} + ${str2}"

//3 boolean 도 가능

let ab = true;
let bc = 1>2;

//4 undefined

let age ;  // 하고 값을 할당하지않으면 undefined 임. 명시적으로 할당하는것도 가능. but , null 사용을 권장

//5 typeof {} 하면 , 타입이나옴.